# =============================================================================
# Cohere API Configuration (for embeddings and answer generation)
# =============================================================================
# How to obtain:
#   1. Sign up at https://cohere.com/
#   2. Navigate to Dashboard → API Keys
#   3. Create a new API key or copy existing one
#   4. Key format: co-xxxxxxxxxxxxxxxxxxxxxxxx
# Required: YES
COHERE_API_KEY=your_cohere_api_key_here

# Cohere model to use for embeddings and text generation
# Recommended: command-r-08-2024 (latest stable model)
# Alternative models: command-r-plus-08-2024, command-r-03-2024
COHERE_MODEL=command-r-08-2024

# =============================================================================
# Qdrant Configuration (for vector storage)
# =============================================================================
# How to obtain:
#   1. Sign up at https://cloud.qdrant.io/
#   2. Create a new cluster (free tier available)
#   3. Go to Cluster Settings → API Keys
#   4. Copy the cluster URL and API key
#   5. URL format: https://xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx.aws.cloud.qdrant.io
# Required: YES
QDRANT_URL=your_qdrant_url_here
QDRANT_API_KEY=your_qdrant_api_key_here

# Collection name must match the collection created in 001-qdrant-retrieval-testing
# Default: rag_embedding
# Note: Collection must be populated with documents before backend can return results
QDRANT_COLLECTION_NAME=rag_embedding

# =============================================================================
# Application Configuration
# =============================================================================
# Target documentation URL (where the Docusaurus site is deployed)
# Production: https://hassaanghayas.github.io/physical-ai-robotics-textbook/
# Local development: http://localhost:3000
TARGET_DOCS_URL=https://hassaanghayas.github.io/physical-ai-robotics-textbook/

# Number of top results to retrieve from vector database
# Range: 1-20, Recommended: 5
# Higher values provide more context but increase response time
TOP_K=5

# Logging level for application logs
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Production: INFO, Development: DEBUG
LOG_LEVEL=INFO

# =============================================================================
# RAG Agent Configuration (Advanced)
# =============================================================================
# Maximum time (seconds) to wait for RAG agent response
# Includes embedding generation, vector search, and answer generation
# Recommended: 30.0 seconds
RAG_AGENT_TIMEOUT=30.0

# Number of retry attempts for failed API calls
# Applies to Cohere and Qdrant API failures
# Recommended: 3
MAX_RETRIES=3

# Circuit breaker threshold (number of failures before opening circuit)
# Prevents cascading failures when external services degrade
# Recommended: 5
CIRCUIT_BREAKER_THRESHOLD=5

# =============================================================================
# Optional: Database Configuration (if using Neon PostgreSQL)
# =============================================================================
# Uncomment and configure if you're using Neon for persistent storage
# How to obtain:
#   1. Sign up at https://neon.tech/
#   2. Create a new project
#   3. Copy the connection string from project settings
#   4. Format: postgresql://user:password@host/database?sslmode=require
# Required: NO (only if using database features)
# NEON_DATABASE_URL=your_neon_postgres_connection_string_here

# =============================================================================
# Security Notes
# =============================================================================
# - NEVER commit .env files with actual secrets to version control
# - This .env.example file should only contain placeholder values
# - Copy this file to .env and fill in your actual values
# - The .gitignore file already prevents .env from being committed
# - Rotate API keys regularly (recommended: every 90 days)
# - Use different keys for development and production environments